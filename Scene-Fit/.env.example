# 사용법: 이 파일을 복사해 `.env`를 만드세요. (`cp .env.example .env`)
# `.env`는 git에 올라가지 않습니다.

# 백엔드 주소. `/v1`은 없어도 프론트가 붙입니다.
# 예: https://mcm-scene-fit-be.vercel.app
VITE_API_BASE_URL=/v1
VITE_API_PROXY_TARGET=http://localhost:8080

# true면 서버를 호출하지 않고 로컬 목업만 사용합니다.
# VITE_API_MOCK=true
